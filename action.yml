# File generated by github.com/posener/goaction. DO NOT EDIT.

name: luet-github-action
description: Github Action for building luet packages and repositories
inputs:
  repo:
    description: "Final container registry repository"
    required: false
  output:
    default: ${PWD}/build
    description: "Output directory for built packages"
    required: false
  luet-version:
    default: "0.36.0"
    description: "Luet version to use"
    required: false
  luet-arch:
    default: "amd64"
    description: "Architecture for Luet binary"
    required: false
  docker-username:
    description: "Docker username to log into"
    required: false
  docker-password:
    description: "Docker password to log into"
    required: false
  docker-endpoint:
    description: "Optional docker endpoint, e.g. quay.io"
    required: false
  FINAL_REPO:
    description: "Final container registry repository (legacy, use repo instead)"
    required: false
  DOCKER_USERNAME:
    description: "Docker username to log into (legacy, use docker-username instead)"
    required: false
  DOCKER_PASSWORD:
    description: "Docker password to log into (legacy, use docker-password instead)"
    required: false
  DOCKER_ENDPOINT:
    description: "Optional docker endpoint (legacy, use docker-endpoint instead)"
    required: false
runs:
  using: docker
  image: Dockerfile
  env:
    FINAL_REPO: "${{ inputs.FINAL_REPO }}"
    DOCKER_USERNAME: "${{ inputs.DOCKER_USERNAME }}"
    DOCKER_PASSWORD: "${{ inputs.DOCKER_PASSWORD }}"
    DOCKER_ENDPOINT: "${{ inputs.DOCKER_ENDPOINT }}"
  args:
  - "-repo=${{ inputs.repo || inputs.FINAL_REPO }}"
  - "-output=${{ inputs.output }}"
  - "-luet-version=${{ inputs.luet-version }}"
  - "-luet-arch=${{ inputs.luet-arch }}"
  - "-docker-username=${{ inputs.docker-username || inputs.DOCKER_USERNAME }}"
  - "-docker-password=${{ inputs.docker-password || inputs.DOCKER_PASSWORD }}"
  - "-docker-endpoint=${{ inputs.docker-endpoint || inputs.DOCKER_ENDPOINT }}"
branding:
  icon: box
  color: purple
